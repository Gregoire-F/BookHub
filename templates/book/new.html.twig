{% extends 'base.html.twig' %}

{% block title %}Nouveau livre{% endblock %}

{% block body %}
<section class="max-w-2xl mx-auto mt-8 px-4">
    <a href="{{ path('app_book_index') }}"
       class="text-sm text-gray-500 hover:text-gray-700 transition-colors mb-6 inline-block">
        ← Retour à la liste
    </a>
    <div class="bg-white rounded-xl shadow p-8">
        <h1 class="font-bold text-2xl text-gray-800 mb-6">Nouveau livre</h1>
        {{ include('book/_form.html.twig') }}
    </div>
</section>
{% endblock %}
